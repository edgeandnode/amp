- name: restore_eth_rpc
  restore: _/eth_rpc@0.0.0

- name: register_joins
  dataset: joins
  tag: "0.0.0"
  end: 15000000

- name: dump_joins
  dataset: _/joins@0.0.0
  end: 15000000

- name: query_join_blocks_txs
  query: |
    SELECT COUNT(*) as count
    FROM joins.join_blocks_txs
  results: |
    [
      {"count": 348}
    ]

- name: query_subquery_alias_table
  query: |
    SELECT _block_num, value
    FROM joins.subquery_alias
  results: |
    [
      {"_block_num": 15000000, "value": "99227573441855374"}
    ]
