{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Manifest",
  "description": "Common metadata fields required by all dataset definitions.",
  "type": "object",
  "properties": {
    "kind": {
      "description": "Dataset kind. See specific dataset definitions for supported values.\n\nCommon values include: `manifest`, `evm-rpc`, `firehose`.",
      "type": "string"
    },
    "network": {
      "description": "Network name, e.g., `mainnet`, `sepolia`\n\nRaw datasets' specific",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "kind"
  ]
}