# Nozzle Example (TypeScript)

Before starting, make sure to set up the `.env` file (see `.env.example`).

## Usage

```sh
bun install

# Build the nozzle dataset manifest
bun nozzl build

# Deploy the nozzle dataset
bun nozzl deploy
```
