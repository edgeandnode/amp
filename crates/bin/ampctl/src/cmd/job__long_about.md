Manage extraction and deployment jobs in the Amp system.

Jobs represent asynchronous data extraction tasks scheduled via the `ampctl dep-dataset` command.
Use these commands to list, inspect, stop, and clean up jobs from the metadata database.
