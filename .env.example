# NOTE: This file is only used in the test suite

# Example environment variables for Nozzle provider configurations
# Copy this file to .env.local and fill in your actual values

# EVM RPC provider URLs
RPC_ETH_MAINNET_URL=https://your-mainnet-rpc-endpoint
RPC_ETH_BASE_URL=https://your-base-rpc-endpoint

# Firehose provider configuration
FIREHOSE_ETH_MAINNET_URL=https://your-firehose-endpoint
FIREHOSE_ETH_MAINNET_TOKEN=your-firehose-api-token

# Add other provider environment variables as needed

# gel db connection
GEL_SERVER_USER=nozzle_registry_adm
GEL_SERVER_PASSWORD=admin
GEL_SERVER_HOST=localhost
GEL_SERVER_PORT=5656
GEL_SERVER_CLIENT_TLS_SECURITY=insecure

# privy auth provider for http-auth e2e test suite

# Privy Auth Configuration
# JWKS JSON used to validate the auth token
# Example: https://auth.privy.io/api/v1/apps/your-app-id/jwks.json
JWKS_URL=
# Privy App ID
AUTH_APP_ID=
# Privy App Secret
AUTH_APP_SECRET=
# Used
AUTH_USER_API_URL=https://api.privy.io/v1/users

# A valid JWT token from your Privy app for testing
# To run auth tests with a real token:
# 1. Get a token by authenticating in your Privy-enabled app
# 2. Set it here and copy this file to .env
# 3. Run: cargo test test_privy_jwt_validation -- --ignored --nocapture
# PRIVY_TEST_TOKEN=eyJhbGciOiJFUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6InNvbWUtaWQifQ...