{
  "name": "@workspace/example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "nozzle": "bun run ../cli/src/cli.ts nozzle",
    "build": "pnpm run nozzle build",
    "deploy": "pnpm run nozzle deploy"
  },
  "dependencies": {
    "@nozzle/cli": "workspace:*"
  }
}
