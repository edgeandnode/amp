{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "All dataset definitions must have a kind, network and name. The name must match the filename. Schema is optional for TOML dataset format.",
  "properties": {
    "kind": {
      "description": "Dataset kind. See specific dataset definitions for supported values.",
      "type": "string"
    },
    "name": {
      "description": "Dataset name.",
      "type": "string"
    },
    "network": {
      "description": "Network name, e.g. \"mainnet\".",
      "type": "string"
    },
    "schema": {
      "additionalProperties": {
        "additionalProperties": {
          "description": "An Arrow data type, e.g., 'Int64', 'Utf8', etc.",
          "type": "string"
        },
        "type": "object"
      },
      "description": "Dataset schema. Lists the tables defined by this dataset.",
      "type": "object"
    }
  },
  "type": "object"
}